<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.filaura.weave.example.system.mapper.UserMapper">

    <resultMap id="SysUserResult" type="User">
        <id     property="id"           column="id"         />
        <result property="username"     column="username"   />
        <result property="nickname"     column="nickname"   />
		<result property="gender"       column="gender"     />
		<result property="password"     column="password"   />
		<result property="avatar"       column="avatar"     />
        <result property="email"        column="email"      />
		<result property="address"      column="address"    />
        <result property="hobbies"  	column="hobbies"    />

        <result property="createBy"     column="create_by" />
        <result property="createTime" 	column="create_time" />
        <result property="updateBy"     column="update_by" />
        <result property="updateTime"   column="update_time" />
        <result property="delFlag"      column="del_flag"   />
    </resultMap>

    <resultMap id="UserWithRoleIdsResult" type="User" extends="SysUserResult">
        <collection property="roleIds" ofType="long">
            <result column="role_id"/>
        </collection>
    </resultMap>

    <select id="selectUserList" resultMap="SysUserResult">
        select id, username, nickname, gender, password, avatar, email, address,
            hobbies, create_by, create_time, update_by, update_time, del_flag
        from sys_user
        where del_flag = 0
    </select>

    <select id="selectUsersWithRoleIds" resultMap="UserWithRoleIdsResult">
        select u.id, u.username, u.nickname, u.gender, u.password, u.avatar, u.email, u.address,
            u.hobbies, u.create_by, u.create_time, u.update_by, u.update_time, u.del_flag,
            sru.role_id
        from sys_user u
        LEFT JOIN sys_role_user sru ON u.id = sru.user_id
        where u.del_flag = 0
    </select>

    <select id="listUserByIds" resultMap="SysUserResult">
        select id, username, nickname, gender, password, avatar, email, address,
            hobbies, create_by, create_time, update_by, update_time, del_flag
        from sys_user
        where del_flag = 0
        and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

</mapper> 